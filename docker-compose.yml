
version: '3'

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: netflix
      POSTGRES_PASSWORD: netflix
      POSTGRES_DB: netflixdb
    ports:
      - "5432:5432"

  redis:
    image: redis
    ports:
      - "6379:6379"
